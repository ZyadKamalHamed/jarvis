# JARVIS Improvement Backlog

Queue for the nightly evolve run (`jobs/jarvis-evolve.md`) and for dev sessions. Top item goes first. One per night for evolve. Anyone (Zyad via `fb:` in the HUD, the daily run, the evolve run itself) can add items; the evolve run grooms and reprioritises. Items marked [dev session] are too large for a single evolve night; they belong to a human-driven session (see `docs/HANDOVER.md` for effort guidance).

1. **Calendar and study ambers: confirm the first green morning.** Both were fixed 27 Jul after Zyad's note that an amber must state its fix or become an operator task. Calendar: the grant was never the problem after 26 Jul, TCC attribution was; a scheduled job or agent shell reads denied while the long-running server reads fine, and the denied doc was overwriting the good one every morning, which is what turned the pipeline amber. `bin/calendar.mjs` now refuses to clobber an ok read under 12 hours old. Study: the vault count moved out of python3 (no folder grant, blocked on tccd with no way to prompt, hung the run three mornings) into `bin/obsidian-sr.mjs`, with the call capped at 20 seconds. Delete this item when a 7:30 morning run reports both ok. If calendar still reads amber, the next step is routing the scheduled read through the running server rather than spawning the helper directly.
2. **Outlook body truncation blunts the high-stakes sweep.** `bin/outlook.py` selects `bodyPreview` and then slices it to 200 characters, so the daily run can see that a mail exists but never its contents. It bit on 28 Jul: a Sonder phone interview booking link from Anna Bramwell, exactly the invite class the binding sweep exists to catch, and the preview cut off at the sentence before the link. The briefing had to say "open it yourself, I cannot read the link", which is the homework the daily run charter forbids. Fix: add a `show <index|subject>` subcommand that selects `body` for one nominated message and prints the plaintext, so a forced act-now hit can be read in full without widening the default fetch payload. Keep the 200-char cap on the list view.

3. **The Mac sleeps through the schedule; check its power posture** [operator]. Evidence keeps stacking: Sunday 19 Jul the half-hourly sweeps never fired all day (the Mac slept; the lock was a bystander), the 7:30 fire slipped two Mondays running, the 23 Jul morning composed at 9:50 via catchup, and overnight into 24 Jul the Mac was offline entirely (AIOS never ran, the evolve job aborted twice, the 7:30 fire was lost, catchup served the day at 9:25). If this Mac is meant to be the always-on box, run `pmset -g` and consider `sudo pmset repeat wakeorpoweron MTWRFSU 07:25:00`, or confirm the always-on role belongs to the home machine and bring the migration forward. Software cannot fix a sleeping computer.
4. **Ask latency: model routing** [dev session]. Every ask spawns claude -p with the full charter, memory and snapshot, so smalltalk pays the same toll as a replan. Design a router in server.mjs ahead of the spawn: a near-instant classifier (keyword heuristic first, tiny-model call only if needed) sends conversational asks ("how are you", catch-ups, questions answerable from the snapshot) to Haiku (`--model claude-haiku-4-5-20251001`) with a slim prompt and no tools, while anything touching the board, drafts, agents or research keeps the full model. Keep sessions warm per model per mode per day so resume cost is paid once. Requested by Zyad by voice 17 Jul. Design first, then implement; measure before/after latency in metrics.jsonl.
5. **Audit the other python entry points for the same folder-access wall.** The study hang was homebrew python3 blocking on `~/Documents` in the scheduled context with no way to prompt. `bin/study.py` (legacy, still runnable) walks the same vault and would hang identically if anything called it; `bin/outlook.py` does not touch protected folders today but would hit this the moment it reads an attachment path. Either give these the same node-helper treatment or wrap every protected read in a bounded call, so the failure mode is always a timeout rather than a stalled morning.
6. **AIOS morning run stalled** [dev session]. Ran fresh 16 Jul after three stale mornings, skipped 18 to 20 Jul, ran fresh 21 Jul, then had produced nothing again by the 22 Jul compose (the daily run re-verified the day's critical links directly instead). The career pipeline keeps carrying forward yesterday's data on bad mornings. The defect lives in ~/Coding/AIOS (its own launchd job), which JARVIS agents must never write to, so this needs Zyad or a dev session over there. Check its joblogs and launchd state first.
7. **Outlook draft live verification.** `bin/outlook.py draft drafts/<file>.md` shipped 13 Jul unit-tested, but the live Graph POST /me/messages call has never been exercised against the real mailbox. One manual run of a real draft file, confirm it appears in the Outlook Drafts folder, then delete this item.
8. **Mail.Send path** [dev session]. Gated on an explicit per-message HUD confirmation design; never buildable by the evolve run alone. HANDOVER has the verified Graph details.
9. **Health workout parsing.** `/api/health` ingests weight and nutrition; extend to the `workouts` array (last session name, weekly count). Real pushes started 26 Jul (Health Auto Export wired, first weight landed), so this is now actionable. Note: nutrition fields arrive empty because he does not log food; the fitness card should keep rendering that honestly rather than as a fault.
10. **Telegram audio delivery.** Attach the briefing mp3 to the morning ping (OpenClaw or bin/notify.sh).
11. **Briefing history archive.** Keep dated copies of briefing.json so past days are browsable.
12. **Google Drive assignment watching.** Populate `context/uni-watchlist.md` and wire the daily run's progressPct evidence checks.
13. **Audio-reactive reactor for music.** Extend the reactor pulse to the boot track and playlist. (Zyad deferred 2026-07-08; never outranks user feedback.)
14. **Playwright MCP enablement** [optional]. `.mcp.json` + user-scoped `enabledMcpjsonServers` so the head agent can drive a browser for logged-out research; deliberately NOT wired for every ask (1 to 3s startup tax, and `open` beats it for anything needing his logins).
15. **Ask failures with no diagnosable cause.** "claude reported an error with no detail" has now logged twice (20 Jul and 26 Jul 4:00am). The ask path's failure logging was hardened 14 Jul yet these two carried nothing actionable. Capture the spawn's exit code and the tail of stderr into the feedback entry when claude exits without a parseable result, so the next repeat can be diagnosed instead of shrugged at.

Done (for the record): impact log via `win:` (2026-07-08), 14-day trend strip (2026-07-08), While You Slept line (2026-07-08), phone layout pass (2026-07-08), conversation memory + LOG overlay (2026-07-09), agentic briefing with actions/proposals/drafts (2026-07-09), head agent with subagents (2026-07-09), tailnet mobile access kit (2026-07-09), launchd scheduling + headless permissions (2026-07-10), long-term agent memory + streaming asks (2026-07-10), weather + capture inbox + health webhook + calendar module (2026-07-10), focus mode + palette + PWA (2026-07-10), backups + Obsidian daily note + hardening (2026-07-10), selfcheck assertion for the /api/dismiss probe-oracle contract (2026-07-11), Canvas whitelisted fetcher: bin/canvas.mjs tracked, covered by the existing `Bash(node bin/*)` allow rule, wired into the daily run, uni pipeline ok since 12 Jul (2026-07-13), ask-path reliability: stdin closed on the claude spawn and failed asks always log a real error (2026-07-14), quick-add stealth vetting on the day board (2026-07-15), friendly ask-failure messages on the HUD with raw errors kept in telemetry (2026-07-16), hands-free voice conversation loop + evolution detail stripped from the work payload (2026-07-17), binding high-stakes email sweep in the daily run so invite-class mail always reaches topThreads and the briefing, first live test on the 20 Jul run (2026-07-19), pid-owned job locks in bin/run-job.sh: a crashed or rebooted run's lock is broken on sight and the next half-hourly sweep reruns the job, a live run is never trampled by age-based breaking and only the wrapper that wrote a lock may release it, verified against seven lock scenarios in an isolated harness (2026-07-20), banned-terms list extended with the four employer targets that had entered career notes and board tasks since the 15 Jul lesson, both spellings of the two-word one (2026-07-21), scheduled daily fire stands down when data/briefing.json already carries today's date, mirroring the catchup check, with --force for the HUD's deliberate re-run button; ends the duplicate agent runs of 17, 18 and 22 Jul (2026-07-22). The Sonder-and-ASD follow-through item retired on schedule: Wednesday 22 Jul happened, prep packs served their day, outcomes tracked in agent memory (2026-07-22). Work mailbox re-authentication confirmed: the 27 Jul run swept both accounts in full after six unreachable mornings, so that watch item retired (2026-07-27).
